#!/bin/sh

if [ $# -ne 0 ]
then
	echo "Usage: $0" >&2
	exit 1
fi

ARCH=$(uname -m)
if [ $ARCH = amd64 ]
then ARCH=x86_64
fi

OS=$(uname -s | tr 'A-Z' 'a-z')
if [ $OS = darwin ]
then OS=freebsd
fi

cat > config.mk <<!
OS_NAME = $OS
ARCH    = $ARCH
!
